const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('#site-nav');menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));document.querySelector('#year').textContent=new Date().getFullYear();const quoteForm=document.querySelector('#quote-form');quoteForm.addEventListener('submit',async event=>{event.preventDefault();const button=quoteForm.querySelector('button[type="submit"]');const message=quoteForm.querySelector('.form-success');button.disabled=true;button.textContent='Sending…';message.hidden=true;message.classList.remove('form-error');try{const response=await fetch(quoteForm.action,{method:'POST',body:new FormData(quoteForm),headers:{Accept:'application/json'}});const result=await response.json();if(!response.ok||!result.success)throw new Error(result.message||'Unable to send request.');message.textContent='Thank you—your quote request has been sent. Our team will follow up shortly.';message.hidden=false;quoteForm.reset()}catch(error){message.textContent='We could not send your request. Please call 888-660-8215 or email Sales@GoAlphaModular.com.';message.classList.add('form-error');message.hidden=false}finally{button.disabled=false;button.textContent='Send Quote Request'}});
